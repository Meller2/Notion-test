# Сюда кладётся 3D-модель

Залей свою модель сюда и назови её **`model.glb`**:

```
public/models/model.glb
```

Если хочешь другое имя — поменяй `MODEL_URL` в `src/main.js`.

Пока файла нет, проект показывает заглушку-торускнот.
